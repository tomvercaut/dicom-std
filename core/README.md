# DICOM-std `core`

This subproject implements a crate to model a subset of data structures described in the DICOM standard and the XML file format in which the standard is defined.

This crate is part of the [DICOM-std](https://github.com/tomvercaut/DICOM-std).

[//]: # (and is contained by the parent crate [DICOM-std]&#40;https://crates.io/crates/dicom-std&#41;.)